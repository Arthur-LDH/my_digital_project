{% extends 'admin/base_admin.html.twig' %}

{% block title %}Edit {{ shop.name }}{% endblock %}

{% block body %}
    <h1>Edit {{ shop.name }}</h1>

    {{ include('admin/shop_crud/_form.html.twig', {'button_label': 'Modifier'}) }}

    <div class="d-flex gap-4 mt-3">
        <a class="btn btn-secondary" href="{{ path('app_admin_shop_index') }}">Retour</a>

        {{ include('admin/shop_crud/_delete_form.html.twig') }}
    </div>
    
{% endblock %}
