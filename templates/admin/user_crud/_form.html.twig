{{ form_start(form) }}
    {{ form_row(form.roles) }}
    {{ form_row(form.login) }}
    {{ form_row(form.email) }}
    {{ form_row(form.plainPassword) }}
    <div {{ stimulus_controller('google-place', {
                            'page': "form",
                            'form': 'user',
                        }) }}></div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
